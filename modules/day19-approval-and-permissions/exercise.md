# Day 19 实践任务

## 任务 1：亲手验证"参数不匹配不该消费掉 nonce"这条设计

在 `src/core/approval.ts` 的 `ApprovalStore.consume()` 里，把 `entry.consumed = true` 这一行往上挪一挪——挪到"是否已消费"那个 `if` 检查**之后**、"是否过期"那个 `if` 检查**之前**（也就是说，跳过了 nonce 存在性 + 是否已消费这两道检查之后，但还没做过期/参数匹配检查，就先把它标记成"已消费"）。重新跑：

```bash
pnpm vitest run tests/approval.test.ts
```

确认"故障注入2：批准后换参数"那条测试失败（其它几条应该还是绿的——想一想为什么只有这一条会受影响，"重放"/"双击批准"那两条为什么不受影响），读一下失败信息（会变成 `ApprovalAlreadyConsumedError`,而不是原本预期的成功）。理解为什么"过期检查/参数匹配检查之前就标记消费"会导致一次合法的重试被误判成"重放"。跑完记得把这一行挪回原来的位置（所有校验都通过之后），确认全绿。

## 任务 2：给 `ApprovalStore` 加一个 `revoke()`

现在一个批准请求只能"被消费"或者"自然过期"，没有办法让创建它的一方主动撤销（比如用户点了批准之后又反悔了，或者上层逻辑发现情况变了、这次批准不该再生效）。给 `ApprovalStore` 加一个 `revoke(nonce: string): void` 方法：

- 撤销一个还没被消费、也没过期的请求之后，`consume()` 对它的调用应该失败（想一想该抛哪种错误——是新建一个 `ApprovalRevokedError`，还是复用现有的某个错误类型？说出你的理由）。
- 撤销一个不存在的 nonce、已经被消费过的 nonce、已经过期的 nonce，应该分别怎么处理？（提示：参考 study.md 第2节的真值表思路，自己先列一张"revoke() 面对四种不同状态分别该怎么反应"的表，再决定要不要抛错、抛哪种）。

补至少三条测试覆盖你列的这张表。

## 任务 3：一道思考题（不用写代码）

如果模型反复对同一个高风险操作发起审批请求（比如连续调用 100 次 `run_command` 触发 100 次审批,每次都被用户拒绝或者压根没人来得及处理),`ApprovalStore.create()` 现在来者不拒。想一想：这算不算 Day18 威胁模型里"资源耗尽"这一类问题的一种？如果要限制,这个限制应该加在 `ApprovalStore` 内部,还是加在"决定要不要为某次工具调用发起审批请求"的那一层（比如权限预设或者以后接进 `runTurn` 的那部分逻辑）？说出理由,可以参照 Day17 任务3"Job 数量上限该放在哪一层"那道题的论证方式。

## 验收自查

- [ ] `pnpm test` 全绿，包括你新增的任务2测试。
- [ ] 我能解释：为什么把 `entry.consumed = true` 挪到校验之前会破坏"参数不匹配不消费 nonce"这条设计,具体是哪条测试、哪一行断言先失败的。
- [ ] 我写完了任务3的思考题，给出了具体的分层建议，不是"看情况"。
