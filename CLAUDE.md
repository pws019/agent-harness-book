# 项目说明（给协作的 Claude 用）

这是一个自学项目：跟着《企业级 Agent / Agent Harness：30 天进阶学习计划》，在 `modules/dayXX-*/` 读文档、做练习，`mini-harness/` 是课程自带的官方参考实现（按天打 tag，目前是 `day01`~`day19`、`phase1-complete`，详见 [mini-harness/README.md](mini-harness/README.md)）。每天目录一般是 `README.md`（学习目标+验收自查）/`study.md`（白话讲解）/`exercise.md`（练习）三件套，Day7/Day14 这类里程碑收尾日没有单独的 `study.md`；`day00-phase1-overview/`、`day08-13-phase2-overview/` 是跨天全貌地图，不是某一天的课；`modules/dayXX-*/answer.md` 是逐条回答该天 README「今天要学会什么」、exercise.md「验收自查」和其中"思考题"的答案，已经补到 Day19。

## 项目当前进度（会过期，动手前先用 `git log`/`ls modules/` 核实）

截至这次更新：Day1-20 全部完成——阶段一（Day1-7）、阶段二（Day8-14）已建完，阶段三（Day15-21，受控执行与人机协作）进行中，已完成 Day15（文件系统能力）、Day16（子进程与 Bash 执行）、Day17（后台任务 `JobRuntime`）、Day18（沙箱与最小权限）、Day19（审批 `ApprovalStore`、权限预设，暂未接进 `runTurn`）、Day20（凭据 `CredentialRef`/`CredentialStore`、分层设置 `mergeSettings`、`WorkspaceContext`，`run_command` 接入 `envCredentials` 示范），正按 `~/.claude/plans/stateless-swinging-kurzweil.md` 里定好的计划继续做 Day21（里程碑三），`mini-harness/` 377 条测试全绿，`mini-harness/CHANGELOG.md` 和两份 `README.md` 的进度表/tag 范围/测试数每天同步更新。阶段三剩余 Day21 还没开始，根 `README.md` 里那部分还是原始大纲文本。

如果要继续往下做新的一天，按已经跑顺的节奏来，不用每天都停下来找我确认：
1. 先读现存代码（不要凭记忆假设，代码可能已经变了），设计这一天要在 `mini-harness/` 里加什么。
2. 实现 + 写测试，`pnpm test`/`pnpm typecheck` 在 `mini-harness/` 里跑到全绿/无错。
3. 写这一天的 `README.md`/`study.md`/`exercise.md`（应用下面"写学习文档时的规则"每一条），再补一份 `answer.md`。
4. 更新 `mini-harness/CHANGELOG.md` 和两份 `README.md` 的进度表/tag范围/测试数。
5. `git tag dayNN`，提交。
6. 没有真正卡住的话，直接接着做下一天，不用每天暂停等我确认。

## 关于我（学习者背景）

- 我是前端开发者，后端/网络相关的基础知识不能默认我懂——比如流式协议、HTTP 分片、TCP/SSE 这类概念需要从头讲清楚原理，不能只讲 DSH 在这些原理之上搭的抽象设计。
- 我更倾向于"先读代码建立直觉，再问问题"，不需要每个概念都提前铺垫讲透——遇到真正卡住的地方我会主动来问。
- 纯设计、无代码的练习（比如 Day1）对我来说比较抽象，需要具体项目例子才能想清楚；一旦进入有代码的天数，理解会快很多。
- 我自己选的毕业项目是"只读代码库调查 Agent"，设计产物在 `docs/`（`product-scope.md`、`adr/001-agent-vs-workflow.md`、`task-catalog.md`）。

## 写学习文档时的规则（study.md / README / exercise.md / answer.md）

这些规则是从实际写文档时被我指出的具体问题里总结出来的，不是泛泛的"写清楚点"——每条都对应一次真实踩过的坑，写之前主动对照检查，不要等我再指出来。

**类型/字段要先展示定义再引用**：写"`SomeType.field` 用来做 X"之前，`SomeType` 的定义（哪怕是裁剪过的）必须已经在同一篇文档里出现过。

**多组件交互，先人话+箭头图，再上代码**：解释"调用方构造 X，注册表查出 Y，Y 转发给 Z"这类流程时，先讲人话说清楚"调用方是谁、在解决什么问题"，画一张简单的 ASCII 箭头图，最后才贴代码，把图上每一步映射回具体符号。

**"哪个信号说了算"这类判断规则要给真值表**：碰到"两个数据源冲突时以谁为准"的判断条件（比如 `toolCalls.length === 0 && finish.kind !== 'tool-calls'`），只讲赢的那一支不够，要把所有输入组合列成真值表。

**跨层传递的值要给"血统追溯"**：一个字段/数值的来源如果对理解设计很关键（id、配置值、算出来的数字），要沿着真实调用链具体点名"哪层产生的、哪层只是转发、哪层最终读取/计算"，标注实际的 file:line，不能停留在"某层转发给下一层"这种抽象描述。

**引用具体代码符号前先 grep 确认它真的存在**：写"某个类/函数叫什么"之前先核实，不要凭印象编一个"应该叫这个"的名字。

**引用一串具体调用序列前先跑一遍确认它真的能跑通**：练习/文档里如果要走一遍具体的多步 API 调用序列（不只是叫一个符号的名字，是链式操作）,先用 `tsx`/`node -e` 之类的方式实际跑一遍，确认它真能到达描述的状态，不会被更早一步的不变式/校验拦下来。

**名字只差标点/大小写的两个类型要显式区分**：比如 `turn-end`（`AgentLoopEvent`，内存里的 generator 事件）和 `turn/end`（`SessionEvent`，真正持久化的日志条目）——这种情况要专门写一句话点破"这是两个不同的东西，只是长得像"，不能只靠格式一致就指望读者自己注意到标点差异。

**新函数先给真实调用场景，再讲机制**：写一个新的纯函数前先 `grep` 一遍代码库看有没有真实调用方；有就直接引用真实代码（file:line，实际打印/做了什么），没有就老实说"目前还没有，是为将来某天预留的"，不要编一个假想场景当真的讲，也不要两种情况混着含糊过去。

**早期悄悄埋下的字段/概念，到用得上的那天要双向补**：如果某天要解释一个类型字段的用途，先查这个字段最初定义的那天有没有被展示/命名过；没有的话回头去最早定义的地方补一个带前向指针的注释（比如 `readonly system?: string // DayN 才会真正讲它是什么`）。引入行业通用术语（RAG、系统提示词、embedding……）时，即使这个词在业内很常见，也要先用类比给出人话定义，不能假设我已经知道。

**README/exercise 里每一条"我能解释 X"都要在 study.md 正文里有直接对应的论证**：写完一天的文档后，逐条对照 README「今天要学会什么」和 exercise.md「验收自查」，为每一条"我能解释 X"/"为什么必须 Y"找到 study.md 里直接论证它的那句话——不是讲了同一个子系统里的另一个相关问题就算数。没有就补一句,不要留着"讲了相关的东西但没真正回答"这种缺口。

**文档正文不能有对话式泄漏**：不能写"我们上一轮聊的场景""刚才提到的"这类引用会话上下文的话——文档是给没参与过这次对话的人看的。也不能用"为什么 X 一定要 Y，不能省"这种直接回答式的小标题去仿照聊天里的一问一答——这预设了"读者刚问了这个问题"，但文档读者从没问过。改成陈述句：先说代码做了什么、去掉会坏在哪、再讲为什么。

**跨天全貌地图（day00 风格文档）必须包含"类型复合图"，不只是调用链图**：调用链图回答"谁调用谁"；类型复合图回答另一个问题——"后面某天定义的类型，哪个字段装着更早某天定义的类型"，用几条命名的"主线"、每条一个从叶子类型往上长的 ASCII 树,标注每个类型是哪天引入的。这是因为自底向上学习天然会积累"类型汤"——每天的类型只在当天的模块里讲清楚,但从没有一张图展示它们跨天怎么复合成一整棵对象图。

## 代码规则

- **我自己加的新注释用中文**，即使这个代码库里课程作者写的原有注释是英文——不要去改动/翻译已经存在的英文注释。
- **改了 `cli.ts`（或任何被 `process.argv` 驱动的胶水层代码）之后，一定要从 shell 里手动跑一遍 `pnpm cli -- ...`**，不能只看 `pnpm test` 绿了就算完——单元测试常常绕开 `main()`/`process.argv` 直接调内部函数，测不出参数解析/子命令分发这一层的问题（Day14 真实踩过：pnpm 某些版本会把 `--` 原样转发进 `process.argv`，子命令判断直接失效，但所有单测都测不出来）。
- **新一天的代码如果调用了更早某天写的函数/模块，要专门写一条跨越这个接缝的集成测试**，不能只分别测两边——Day9（`closeDanglingActivity` 忘了走 sink 转发）、Day13（重复压缩产生的区间重叠）、Day14（截断恢复后续写把日志弄得更糟）三个真实 bug，都是"孤立测每一边都通过，只有把两边串起来跑一遍才会暴露"的类型，具体清单见 `mini-harness/CHANGELOG.md` 对应天数。
